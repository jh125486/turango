{
  "description": "Minimal integration fixture for literal/boolean: FeatureEnabled's `return true` is never exercised by any test, so the true/false swap survives. Deliberately the smallest possible fixture (one mutant) to isolate this operator cleanly.",
  "modulePath": "corpus/op-literal-boolean/module",
  "scope": "package",
  "operators": null,
  "timeout": "15s",
  "expect": {
    "mutants": 1,
    "killed": 0,
    "survived": 1,
    "notViable": 0
  }
}
